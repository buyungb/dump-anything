# Origin of the Rust API. Leave empty (or unset) when the dashboard is
# served by nginx in the bundled production image — fetches go to the
# same origin and nginx proxies them. Set this for split local dev.
VITE_API_URL=http://localhost:3000
